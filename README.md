# Route Analyzer ![Python](https://img.shields.io/badge/python-3670A0?style=for-the-badge&logo=python&logoColor=ffdd54) ![Performance](https://img.shields.io/badge/performance-optimized-green?style=for-the-badge)

Высокопроизводительный инструмент для анализа маршрутов и построения карт для логистических задач. Позволяет рассчитывать расстояния между точками доставки и визуализировать маршруты на интерактивной карте с **параллельной обработкой и интеллектуальным кэшированием**.

## 🚀 Возможности 

- 📊 **Матрица расстояний** - автоматический расчет расстояний между всеми точками
- 🗺️ **Интерактивные карты** - визуализация маршрутов с помощью Folium
- 🚛 **Оптимизация для грузовиков** - использование профиля `driving-hgv` в OpenRouteService
- ⚡ **Параллельная обработка** - многопоточные API запросы для максимальной скорости
- 💾 **Интеллектуальное кэширование** - сохранение результатов для мгновенных повторных запусков
- 🔄 **Улучшенная обработка ошибок** - быстрые retry с оптимизированными задержками
- 📈 **Экспорт данных** - сохранение результатов в Excel и CSV форматах
- ⚙️ **Гибкая настройка** - настраиваемое количество потоков для разных задач

## Установка 

### Требования
- Python 3.7+
- API ключ OpenRouteService

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Настройка API ключа

1. Получите бесплатный API ключ на [OpenRouteService](https://openrouteservice.org/)
2. Замените ключ в файле `API_ORS_key.py`:

```python
orskey = "ваш_api_ключ_здесь"
```

## Использование 

### Базовое использование

```bash
# Запуск полного анализа (карта + матрица)
python route_analyzer.py

# Только создание карты
python route_analyzer.py --mode map

# Только построение матрицы расстояний
python route_analyzer.py --mode matrix

# Очистка временных файлов и запуск
python route_analyzer.py --clean
```

### Продвинутые параметры производительности

```bash
# Увеличение потоков для матрицы расстояний (по умолчанию: 3)
python route_analyzer.py --workers-matrix 5

# Увеличение потоков для построения карты (по умолчанию: 2)
python route_analyzer.py --workers-map 4

# Комбинированная настройка
python route_analyzer.py --workers-matrix 4 --workers-map 3 --mode both

# Очистка кэша и временных файлов
python route_analyzer.py --clean
```

### Параметры командной строки

- `--mode {map,matrix,both}` - Режим работы (по умолчанию: both)
- `--clean` - Очистить временные файлы и кэш перед началом
- `--workers-matrix` - Количество потоков для матрицы расстояний (по умолчанию: 3)
- `--workers-map` - Количество потоков для построения карты (по умолчанию: 2)

## Структура проекта 

```
├── route_analyzer.py      # Основной скрипт (объединенная функциональность)
├── points.py              # Координаты точек доставки
├── API_ORS_key.py         # API ключ OpenRouteService
├── requirements.txt       # Зависимости Python
├── README.md             # Документация
└── .gitignore           # Исключения для Git
```

## Выходные файлы 

- `all_routes_map.html` - Интерактивная карта с маршрутами
- `distance_matrix.xlsx` - Матрица расстояний в Excel
- `distance_matrix_partial.csv` - Промежуточные результаты
- `distance_cache.json` - Кэш расстояний для ускорения повторных запусков
- `route_cache.json` - Кэш маршрутов для быстрой регенерации карт

## ⚡ Производительность и оптимизация

### Ускорение работы

- **Параллельная обработка**: 2-3x быстрее за счет многопоточных API запросов
- **Интеллектуальное кэширование**: 10-50x быстрее при повторных запусках
- **Оптимизированные retry**: Уменьшенные задержки при ошибках API
- **Эффективное сохранение**: Реже обращается к файловой системе

### Рекомендации по настройке потоков

```bash
# Для мощных систем (8+ CPU cores)
python route_analyzer.py --workers-matrix 6 --workers-map 4

# Для стандартных систем (4-8 CPU cores)  
python route_analyzer.py --workers-matrix 4 --workers-map 3

# For слабых систем (2-4 CPU cores)
python route_analyzer.py --workers-matrix 2 --workers-map 1
```

### Управление кэшем

Кэш автоматически сохраняется каждые 5 обработанных элементов:
- Расстояния кэшируются в `distance_cache.json`
- Маршруты кэшируются в `route_cache.json`
- Используйте `--clean` для полной очистки кэша

## Настройка точек доставки 

Отредактируйте файл `points.py` для добавления ваших точек:

```python
points = {
    "Склад": (55.852864, 37.402408),
    "Точка 1": (55.843636, 37.444027),
    "Точка 2": (55.846239, 37.453199),
    # ... добавьте свои точки
}
```

## Ограничения API ⚠️

- OpenRouteService имеет лимиты на количество запросов (обычно 2000 запросов/день для бесплатного аккаунта)
- **Оптимизированная обработка**: Скрипт эффективно использует лимиты через параллельные запросы и кэширование
- **Умные retry**: Автоматическая обработка превышения лимитов с минимальными задержками (5-15 секунд)
- **Эффективное использование кэша**: Повторные запуски практически не потребляют API квоту
- Для больших наборов данных первый запуск может занять время, но последующие будут практически мгновенными
- Рекомендуется запускать с оптимальным количеством потоков для вашей системы

## 📊 Примеры использования 

### Логистическая компания (максимальная производительность)
```bash
# Анализ маршрутов для 15 точек доставки с оптимизацией
python route_analyzer.py --workers-matrix 4 --workers-map 3 --mode both
```

### Быстрое создание карты с кэшированием
```bash
# Только визуализация с использованием кэша маршрутов
python route_analyzer.py --mode map --workers-map 2
```

### Пересчет с очисткой кэша
```bash
# Удаление старых результатов и полный пересчет
python route_analyzer.py --clean --mode both
```

### Тестирование производительности
```bash
# Максимальная производительность для мощных систем
python route_analyzer.py --workers-matrix 6 --workers-map 4
```

## Технические детали 🔧

- **API**: OpenRouteService для расчета маршрутов
- **Карты**: Folium для интерактивной визуализации
- **Данные**: Pandas для обработки матриц расстояний
- **Параллелизм**: ThreadPoolExecutor для многопоточной обработки
- **Кэширование**: JSON файлы с thread-safe доступом
- **Профиль**: `driving-hgv` для грузовиков
- **Форматы**: HTML, Excel, CSV, JSON

## 🆕 Обновления производительности

### Версия 2.0 - Оптимизированная производительность
- ✅ **Параллельная обработка** - Многопоточные API запросы
- ✅ **Интеллектуальное кэширование** - Мгновенные повторные запуски  
- ✅ **Оптимизированные retry** - Быстрое восстановление после ошибок
- ✅ **Настраиваемые потоки** - Гибкая настройка под систему
- ✅ **Улучшенное сохранение** - Эффективная работа с файлами

### Сравнение производительности
- **Первый запуск**: 2-3x быстрее
- **Повторные запуски**: 10-50x быстрее
- **Использование API**: Эффективное использование лимитов
- **Надежность**: Улучшенная обработка ошибок

## Поддержка 

При возникновении проблем:
1. Проверьте правильность API ключа
2. Убедитесь в корректности координат в `points.py`
3. Проверьте подключение к интернету
4. Попробуйте уменьшить количество потоков при ошибках rate limit
5. Используйте `--clean` для сброса кэша при проблемах
6. Обратитесь к документации OpenRouteService

---

**Создано для оптимизации логистических маршрутов с максимальной производительностью** 🚛⚡✨
